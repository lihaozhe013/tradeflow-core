{
  "name": "tradeflow-core",
  "private": true,
  "version": "0.6.0",
  "description": "a lightweight tradeflow system core",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && npx prisma generate",
    "ci:all": "npm ci && cd frontend && npm ci && cd ../backend && npm ci && npx prisma generate",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "node \"scripts/build/pre-build.js\" && cd frontend && npm run build && cd ../backend && npm run build && cd .. && npm run build:post-build",
    "build:post-build": "node \"./scripts/build/post-build-global.js\"",
    "start": "cd dist && cross-env NODE_ENV=production backend/server/.js"
  },
  "devDependencies": {
    "argon2": "^0.44.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "fs-extra": "^11.3.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=11.0.0"
  }
}
